# Boîte de dépôt automatique « Québec en 360 » — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Déposer un clip, une photo ou un pano fini dans un dossier partagé du Mac Pro NAS publie automatiquement un lieu géolocalisé sur `theo-picture.com/labs/360`.

**Architecture:** On extrait le cœur réutilisable de `scripts/iso360.sh` (géoloc, publication tunnel, câblage `labs360.ts`, build garde-fou, commit/push) dans une bibliothèque `source`-able `scripts/iso360-core.sh`. Un nouveau `scripts/iso-ingest.sh` (déclenché par un LaunchAgent `WatchPaths`) détecte le type de fichier, lit le GPS via `exiftool`, et délègue au cœur partagé. Le site gagne un type de pin `photo` (lightbox `<img>`).

**Tech Stack:** Bash, Python 3 (stdlib), exiftool, ffmpeg, sips, launchd (macOS), Astro 6, GSAP, Pannellum, MapKit JS.

## Global Constraints

- **Node** `>=22.12.0`. `npm run build` doit sortir **exactement 10 pages** sans erreur — c'est le garde-fou avant chaque push.
- **Marque** : jamais « Théo Picture » dans le contenu visible ; « le studio » / « ISO Nord ».
- **Source de vérité** : `src/data/labs360.ts` (tableau TS versionné git). Pas de vraie base de données.
- **Cache média 1 an** : ne JAMAIS réutiliser un nom de fichier média publié. Noms versionnés `<id>-<AAAA-MM>.<ext>`.
- **Ne jamais publier un pin à un endroit deviné** : sans lieu fiable → quarantaine `inbox-corriger/`.
- **Dossier surveillé isolé** : quarantaine/archive/log sont des **frères** de `inbox/`, jamais dedans (sinon boucle `WatchPaths`).
- **Contrat DOM** (ne pas casser, cf `labs360.js`) : `[data-place-id]`, `#l360-modal`, `#l360-media`, `#l360-title`, `.l360-modal__{desc,credit,hint,fallback}`, `[data-modal-close]`, `<script id="l360-data">`.
- Chemins infra : repo `~/Desktop/Developer/iso-nord` ; tunnel `/Volumes/SSD 1/iso-nord-media/` ; base publique `https://media.theo-picture.com` ; page live `https://theo-picture.com/labs/360`.
- Fin des messages de commit : `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

## File Structure

| Fichier | Création / Modif | Responsabilité |
|---|---|---|
| `scripts/iso360-core.sh` | Créer | Bibliothèque `source`-able : `core_extract_meta`, `core_geocode`, `core_forward_geocode`, `core_publish_verify`, `core_wire`, `core_build_guard`, `core_commit_push`. |
| `scripts/iso360.sh` | Modifier | `source` le cœur ; garde le stitch DJI + le mode fichier. Comportement CLI inchangé. |
| `scripts/iso-ingest.sh` | Créer | Ingesteur : détection type, GPS exiftool, préparation média (poster ffmpeg), quarantaine, lock, log ; délègue au cœur. |
| `launchd/com.iso-nord.inbox.plist` | Créer | LaunchAgent `WatchPaths` sur `inbox/` → `iso-ingest.sh`. |
| `src/data/labs360.ts` | Modifier | `PlaceType = '360' \| 'video' \| 'photo'`. |
| `src/i18n/ui.ts` | Modifier | `labs360.badgePhoto` (fr + en). |
| `src/components/pages/Labs360.astro` | Modifier | `runtime.badgePhoto`, badge légende 3 voies, CSS lightbox `img`. |
| `src/scripts/labs360.js` | Modifier | `mountMedia` branche `photo` (`<img>`), glyph carte 3 voies, badge 3 voies. |
| `docs/HANDOFF-codex.md` | Modifier | Section « boîte de dépôt » : install LaunchAgent, dossiers, dépannage. |

**Note sur les tests :** le projet n'a **aucun framework de test** (pas de vitest/jest/bats). La vérification se fait donc par : (a) `npm run build` → 10 pages pour toute modif du site ; (b) exécutions fonctionnelles `--dry-run` / `--no-push` pour les scripts, avec des fichiers-fixtures ; (c) vérification navigateur pour l'UI. Chaque tâche précise sa commande de vérification et la sortie attendue.

---

## Task 1: Provisionner dépendances + dossiers de la boîte de dépôt

**Files:** aucun fichier du repo (état système + dossiers sur le SSD). Pas de commit.

**Interfaces:**
- Produces : binaires `exiftool`, `ffmpeg` dans le PATH ; dossiers `/Volumes/SSD 1/iso-nord-media/{inbox,inbox-corriger,inbox-publies}/` ; convention log `/Volumes/SSD 1/iso-nord-media/inbox.log`.

- [ ] **Step 1: Installer exiftool + ffmpeg**

Run:
```bash
brew install exiftool ffmpeg
```

- [ ] **Step 2: Vérifier l'installation**

Run:
```bash
exiftool -ver && ffmpeg -version | head -1
```
Expected: un numéro de version exiftool (ex. `12.xx`) puis une ligne `ffmpeg version …`. Les deux commandes réussissent (exit 0).

- [ ] **Step 3: Créer les dossiers de la boîte de dépôt (si le SSD est monté)**

Run:
```bash
BASE="/Volumes/SSD 1/iso-nord-media"
if [ -d "$BASE" ]; then
  mkdir -p "$BASE/inbox" "$BASE/inbox-corriger" "$BASE/inbox-publies" "$BASE/photos"
  ls -d "$BASE"/inbox "$BASE"/inbox-corriger "$BASE"/inbox-publies "$BASE"/photos
else
  echo "SSD non monté — les dossiers seront créés au premier run par iso-ingest.sh"
fi
```
Expected: soit les 4 chemins listés, soit le message « SSD non monté ». (Le SSD peut être absent sur une machine de dev ; `iso-ingest.sh` recrée les dossiers à l'exécution.)

- [ ] **Step 4: Pas de commit** — cette tâche ne modifie aucun fichier du repo. Passer à la Task 2.

---

## Task 2: Type de pin `photo` sur le site

**Files:**
- Modify: `src/data/labs360.ts:18` (union `PlaceType`)
- Modify: `src/i18n/ui.ts:176` et `:399` (ajout `badgePhoto`)
- Modify: `src/components/pages/Labs360.astro:50` (runtime), `:137` (badge légende), `:364` (CSS)
- Modify: `src/scripts/labs360.js:130` (glyph + subtitle), `:187`/`:215` (mountMedia)

**Interfaces:**
- Produces : le type `'photo'` accepté par `Labs360Place`, rendu comme lightbox `<img>` dans la modale et pin `◆` sur la carte. Consommé par `core_wire` (Task 4) qui écrira `type: 'photo'`.

- [ ] **Step 1: Élargir l'union de type dans `labs360.ts`**

Dans `src/data/labs360.ts`, remplacer la ligne 18 :
```ts
export type PlaceType = '360' | 'video';
```
par :
```ts
export type PlaceType = '360' | 'video' | 'photo';
```

- [ ] **Step 2: Ajouter le badge i18n (FR)**

Dans `src/i18n/ui.ts`, après la ligne `badgeVideo: 'Clip',` du bloc **fr** (ligne 176), ajouter :
```ts
      badgePhoto: 'Photo',
```

- [ ] **Step 3: Ajouter le badge i18n (EN)**

Dans `src/i18n/ui.ts`, après la ligne `badgeVideo: 'Clip',` du bloc **en** (ligne 399), ajouter :
```ts
      badgePhoto: 'Photo',
```

- [ ] **Step 4: Exposer `badgePhoto` au runtime dans `Labs360.astro`**

Dans `src/components/pages/Labs360.astro`, dans l'objet `runtime` (autour de la ligne 49-50), remplacer :
```ts
  badge360: l.badge360,
  badgeVideo: l.badgeVideo,
};
```
par :
```ts
  badge360: l.badge360,
  badgeVideo: l.badgeVideo,
  badgePhoto: l.badgePhoto,
};
```

- [ ] **Step 5: Badge de légende à 3 voies dans `Labs360.astro`**

Dans `src/components/pages/Labs360.astro` ligne 137, remplacer :
```astro
                    <em class="l360-legend__badge">{p.type === '360' ? l.badge360 : l.badgeVideo}</em>
```
par :
```astro
                    <em class="l360-legend__badge">{p.type === '360' ? l.badge360 : p.type === 'photo' ? l.badgePhoto : l.badgeVideo}</em>
```

- [ ] **Step 6: Styler l'image lightbox dans `Labs360.astro`**

Dans `src/components/pages/Labs360.astro`, remplacer le bloc CSS (lignes 359-364) :
```css
      .l360-modal__media :global(video),
      .l360-modal__media :global(.pnlm-container) {
        position: absolute !important; inset: 0;
        width: 100% !important; height: 100% !important;
      }
      .l360-modal__media :global(video) { object-fit: cover; }
```
par :
```css
      .l360-modal__media :global(video),
      .l360-modal__media :global(.pnlm-container),
      .l360-modal__media :global(img) {
        position: absolute !important; inset: 0;
        width: 100% !important; height: 100% !important;
      }
      .l360-modal__media :global(video) { object-fit: cover; }
      .l360-modal__media :global(img) { object-fit: contain; background: #0c0b0f; }
```

- [ ] **Step 7: Glyph carte + subtitle à 3 voies dans `labs360.js`**

Dans `src/scripts/labs360.js`, dans `initMapKit`, remplacer le bloc `MarkerAnnotation` (lignes 126-131) :
```js
    const ann = new mapkit.MarkerAnnotation(new mapkit.Coordinate(p.lat, p.lon), {
      color: '#c8ff00',
      glyphText: p.type === '360' ? '◉' : '▶',
      title: p.name,
      subtitle: p.type === '360' ? DATA.badge360 : DATA.badgeVideo,
    });
```
par :
```js
    const glyph = p.type === '360' ? '◉' : p.type === 'photo' ? '◆' : '▶';
    const badge = p.type === '360' ? DATA.badge360 : p.type === 'photo' ? DATA.badgePhoto : DATA.badgeVideo;
    const ann = new mapkit.MarkerAnnotation(new mapkit.Coordinate(p.lat, p.lon), {
      color: '#c8ff00',
      glyphText: glyph,
      title: p.name,
      subtitle: badge,
    });
```

- [ ] **Step 8: Brancher le rendu `photo` dans `mountMedia` (`labs360.js`)**

Dans `src/scripts/labs360.js`, dans `mountMedia`, remplacer le bloc `else` final (lignes 215-224) :
```js
  } else {
    const video = document.createElement('video');
    video.controls = true;
    video.playsInline = true;
    video.preload = 'metadata';
    if (place.poster) video.poster = place.poster;
    video.src = place.media;
    video.addEventListener('error', showFallback, { once: true });
    mediaHost.append(video);
  }
```
par :
```js
  } else if (place.type === 'photo') {
    const img = document.createElement('img');
    img.loading = 'lazy';
    img.alt = place.name;
    img.src = place.media;
    img.addEventListener('error', showFallback, { once: true });
    mediaHost.append(img);
  } else {
    const video = document.createElement('video');
    video.controls = true;
    video.playsInline = true;
    video.preload = 'metadata';
    if (place.poster) video.poster = place.poster;
    video.src = place.media;
    video.addEventListener('error', showFallback, { once: true });
    mediaHost.append(video);
  }
```

- [ ] **Step 9: Ajouter une entrée photo TEMPORAIRE pour vérifier le rendu**

Dans `src/data/labs360.ts`, juste avant la ligne `  // iso360:insert`, insérer ce bloc de test (utilise une image déjà présente dans `public/`) :
```ts
  {
    id: 'photo-test',
    city: 'quebec',
    type: 'photo',
    name: 'Photo test',
    desc: { fr: 'Entrée de test — à retirer.', en: 'Test entry — to remove.' },
    credit: '',
    lat: 46.8139, lon: -71.2080,
    media: '/assets/portfolio/chute-automne.jpeg',
  },
```

- [ ] **Step 10: Build de vérification**

Run:
```bash
npm run build 2>&1 | tail -5
```
Expected: build réussi, mention de **10 pages** (`10 page(s) built`), aucune erreur TypeScript.

- [ ] **Step 11: Vérification navigateur (preview)**

Démarrer le preview et ouvrir `/labs/360` (repli légende, MapKit ne s'authentifie pas en local). Cliquer l'item de légende « Photo test » : la modale doit afficher l'image (lightbox `object-fit: contain`), pas un `<video>` ni Pannellum. Le badge de légende affiche « Photo ».

Run:
```bash
npm run dev
```
Puis, dans le navigateur du preview : ouvrir `http://localhost:4321/labs/360`, cliquer « Photo test » dans la liste des lieux, confirmer l'image affichée. (Vérifier aussi la console : aucune erreur.)

- [ ] **Step 12: Retirer l'entrée de test**

Dans `src/data/labs360.ts`, supprimer le bloc `id: 'photo-test'` ajouté au Step 9 (le type `photo` reste supporté, seule l'entrée de démo part).

- [ ] **Step 13: Re-build pour confirmer 8→10 pages sans l'entrée test**

Run:
```bash
npm run build 2>&1 | tail -3
```
Expected: **10 pages**, aucune erreur.

- [ ] **Step 14: Commit**

```bash
git add src/data/labs360.ts src/i18n/ui.ts src/components/pages/Labs360.astro src/scripts/labs360.js
git commit -m "feat(labs360): support 'photo' pin type (lightbox img + carte ◆)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3: Bibliothèque partagée `iso360-core.sh` + refactor `iso360.sh`

**Files:**
- Create: `scripts/iso360-core.sh`
- Modify: `scripts/iso360.sh` (remplace les blocs géoloc/publie/câble/build/push par des appels aux fonctions du cœur)

**Interfaces:**
- Produces (fonctions `source`-ables) :
  - `core_extract_meta <file>` → écrit sur stdout `LAT LON DT` (champs vides si absents). Utilise exiftool.
  - `core_geocode` → lit env `LAT LON DT NAME_OVR CITY_OVR ID_OVR`, écrit le **GEO_JSON** sur stdout (clés : `lat lon dt ym name city id display`).
  - `core_forward_geocode <query>` → écrit `LAT LON` sur stdout (vide si introuvable).
  - `core_publish_verify <local> <destdir> <baseurl> <filename>` → copie + vérifie HTTP 200 ; exit ≠ 0 si échec.
  - `core_wire` → lit env `GEO_JSON MEDIA_URL PTYPE POSTER_URL REPLACE WIRE_NOTE DATA` ; insère/remplace un lieu dans `labs360.ts`.
  - `core_build_guard <data_file>` → `npm run build` ; rollback + exit ≠ 0 si échec.
  - `core_commit_push <data_file> <verb> <name> <push> <live_page> <filename>` → commit (+ push si `push=1`).
- Consumed by : Task 4 (`iso-ingest.sh`) et `iso360.sh` refactoré.

- [ ] **Step 1: Créer `scripts/iso360-core.sh` avec les fonctions partagées**

Créer `scripts/iso360-core.sh` :
```bash
#!/usr/bin/env bash
#
# iso360-core — fonctions partagées entre iso360 (manuel, stitch DJI) et
# iso-ingest (auto, boîte de dépôt). À `source` — ne s'exécute pas seul.
#
# Contrat : les fonctions écrivent leur résultat sur stdout et renvoient un
# code de sortie ≠ 0 en cas d'échec. Les logs vont sur stderr.

# ─── Log helpers (repris de iso360.sh) ───────────────────────────────────────
core_b(){ printf '\033[1m%s\033[0m\n' "$*" >&2; }
core_ok(){ printf '\033[32m✓\033[0m %s\n' "$*" >&2; }
core_info(){ printf '\033[36m•\033[0m %s\n' "$*" >&2; }
core_warn(){ printf '\033[33m⚠ %s\033[0m\n' "$*" >&2; }

# core_extract_meta <file> → "LAT LON DT" (champs vides si absents)
# exiftool lit le GPS des JPEG (drone/photo) ET des MP4/MOV. Sortie JSON avec
# labels → on ne dépend pas de l'ordre/du nombre de lignes.
core_extract_meta() {
  exiftool -n -j -GPSLatitude -GPSLongitude -DateTimeOriginal -CreateDate "$1" 2>/dev/null \
  | python3 -c '
import sys, json
try:
    d=json.load(sys.stdin)[0]
except Exception:
    print("  "); sys.exit(0)
lat=d.get("GPSLatitude",""); lon=d.get("GPSLongitude","")
dt=d.get("DateTimeOriginal") or d.get("CreateDate") or ""
print(f"{lat} {lon} {dt}")
'
}

# core_geocode : env LAT LON DT NAME_OVR CITY_OVR ID_OVR → GEO_JSON sur stdout
core_geocode() {
  python3 <<'PY'
import os, json, urllib.request, urllib.parse, re, unicodedata
def slugify(s):
    s=unicodedata.normalize('NFKD',s).encode('ascii','ignore').decode()
    return re.sub(r'-+','-',re.sub(r'[^a-z0-9]+','-',s.lower())).strip('-')
def num(v):
    v=(v or '').strip()
    try: return float(v)
    except ValueError: return None
lat=num(os.environ.get('LAT')); lon=num(os.environ.get('LON'))
dt=(os.environ.get('DT') or '').strip()
ym=dt[:7].replace(':','-') if dt else ''
res={'lat':lat,'lon':lon,'dt':dt,'ym':ym}
name=os.environ.get('NAME_OVR','').strip()
city=os.environ.get('CITY_OVR','').strip()
if lat is not None and lon is not None:
    try:
        u='https://nominatim.openstreetmap.org/reverse?'+urllib.parse.urlencode(
            {'lat':lat,'lon':lon,'format':'json','accept-language':'fr','zoom':'17'})
        req=urllib.request.Request(u,headers={'User-Agent':'iso360/1.0 (theo-picture.com)'})
        g=json.load(urllib.request.urlopen(req,timeout=20)); a=g.get('address',{})
        res['display']=g.get('display_name','')
        if not name:
            for k in ('tourism','leisure','building','amenity','neighbourhood',
                      'suburb','quarter','city_district','road','hamlet'):
                if a.get(k): name=a[k]; break
        if not city:
            city='quebec' if lat>46.3 else 'montreal'
    except Exception as e:
        res['geo_error']=str(e)
if not city: city='quebec'
if not name: name='Lieu sans nom'
res.update({'name':name,'city':city,
            'id':os.environ.get('ID_OVR','').strip() or slugify(name)})
print(json.dumps(res,ensure_ascii=False))
PY
}

# core_forward_geocode <query> → "LAT LON" (vide si introuvable)
core_forward_geocode() {
  QUERY="$1" python3 <<'PY'
import os, json, urllib.request, urllib.parse
q=os.environ['QUERY'].replace('-',' ').strip()
if not q: raise SystemExit
u='https://nominatim.openstreetmap.org/search?'+urllib.parse.urlencode(
    {'q':q,'format':'json','limit':'1','accept-language':'fr','countrycodes':'ca'})
req=urllib.request.Request(u,headers={'User-Agent':'iso360/1.0 (theo-picture.com)'})
try:
    r=json.load(urllib.request.urlopen(req,timeout=20))
    if r: print(f"{r[0]['lat']} {r[0]['lon']}")
except Exception:
    pass
PY
}

# core_publish_verify <local> <destdir> <baseurl> <filename> → exit≠0 si échec
core_publish_verify() {
  local local_file="$1" destdir="$2" baseurl="$3" filename="$4"
  [[ -d "$destdir" ]] || { core_warn "Dossier média absent : $destdir (SSD monté ?)"; return 1; }
  cp "$local_file" "$destdir/$filename" || return 1
  core_ok "Copié dans $destdir/$filename"
  local code=""
  for _ in 1 2 3 4 5; do
    code=$(curl -s -o /dev/null -w '%{http_code}' "$baseurl/$filename" || true)
    [[ "$code" == "200" ]] && break
    sleep 3
  done
  [[ "$code" == "200" ]] || { core_warn "URL publique répond $code : $baseurl/$filename"; return 1; }
  core_ok "En ligne : $baseurl/$filename"
}

# core_wire : env GEO_JSON MEDIA_URL PTYPE POSTER_URL REPLACE WIRE_NOTE DATA
core_wire() {
  python3 <<'PY'
import os, json, re
g=json.loads(os.environ['GEO_JSON'])
url=os.environ['MEDIA_URL']; rep=os.environ.get('REPLACE','').strip()
path=os.environ['DATA']; typ=os.environ.get('PTYPE','360')
poster=os.environ.get('POSTER_URL','').strip()
note=os.environ.get('WIRE_NOTE','Auto-publié')
src=open(path,encoding='utf-8').read()
if rep:
    pat=re.compile(r"(\{\s*\n\s*id:\s*'"+re.escape(rep)+r"'.*?media:\s*)'[^']*'",re.S)
    if not pat.search(src): raise SystemExit(f"Lieu introuvable pour --replace {rep}")
    src=pat.sub(lambda m: m.group(1)+json.dumps(url), src, count=1)
    open(path,'w',encoding='utf-8').write(src); print(f"replaced:{rep}")
else:
    nm=json.dumps(g['name'])
    dfr=json.dumps(f"{g['name']} — vue aérienne captée au drone.")
    den=json.dumps(f"{g['name']} — aerial view captured by drone.")
    lat=g.get('lat') if g.get('lat') is not None else (46.85 if g['city']=='quebec' else 45.51)
    lon=g.get('lon') if g.get('lon') is not None else (-71.15 if g['city']=='quebec' else -73.57)
    poster_line=f"    poster: {json.dumps(poster)},\n" if poster else ""
    block=(f"  {{\n    id: {json.dumps(g['id'])},\n    city: {json.dumps(g['city'])},\n"
           f"    type: {json.dumps(typ)},\n    name: {nm},\n    desc: {{\n      fr: {dfr},\n      en: {den},\n    }},\n"
           f"    credit: '',\n    lat: {lat}, lon: {lon},\n"
           f"    // {note} ({g.get('dt','')})\n"
           f"    media: {json.dumps(url)},\n{poster_line}  }},\n")
    marker="  // iso360:insert"
    if marker not in src: raise SystemExit("Marqueur iso360:insert absent de labs360.ts")
    src=src.replace(marker, block+marker, 1)
    open(path,'w',encoding='utf-8').write(src); print(f"inserted:{g['id']}")
PY
}

# core_build_guard <data_file> → rollback + exit≠0 si le build casse
core_build_guard() {
  local data_file="$1"
  core_info "Build de validation…"
  if ! npm run build >/tmp/iso360-build.log 2>&1; then
    git checkout -- "$data_file"
    core_warn "Build échoué → $(basename "$data_file") restauré, rien poussé. Voir /tmp/iso360-build.log"
    return 1
  fi
  core_ok "Build OK (10 pages)"
}

# core_commit_push <data_file> <verb> <name> <push> <live_page> <filename>
core_commit_push() {
  local data_file="$1" verb="$2" name="$3" push="$4" live_page="$5" filename="$6"
  git add "$data_file"
  git commit -q -m "feat(labs360): $verb '$name' (iso-ingest, auto-geo)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
  core_ok "Commit créé"
  [[ "$push" == "1" ]] || { core_b "Commit local (pas de push)."; return 0; }
  git push -q origin main
  core_ok "Poussé → déploiement Vercel en cours"
  for _ in $(seq 1 18); do
    if curl -s -H 'Cookie: lang=fr' "$live_page" | grep -q "$filename"; then
      curl -s -o /dev/null "$MEDIA_BASE_URL/$filename" 2>/dev/null || true
      core_b "🚁 EN LIGNE : $live_page"
      return 0
    fi
    sleep 10
  done
  core_b "Poussé ✓ — propagation en cours. Vérifie $live_page dans une minute."
}
```

- [ ] **Step 2: Vérifier que le cœur se `source` et que les fonctions existent**

Run:
```bash
bash -c 'source scripts/iso360-core.sh && declare -F | grep -Eo "core_[a-z_]+" | sort'
```
Expected: liste incluant `core_build_guard core_commit_push core_extract_meta core_forward_geocode core_geocode core_publish_verify core_wire`.

- [ ] **Step 3: Vérifier `core_extract_meta` sur un JPEG géotagué**

Trouver un JPEG avec GPS (ex. un segment DJI de la carte SD si montée, sinon toute photo géotaguée) et lire ses métadonnées :
```bash
source scripts/iso360-core.sh
core_extract_meta "/chemin/vers/photo-geotaguee.jpg"
```
Expected: une ligne `LAT LON DATE` (ex. `46.83270000 -71.24450000 2026:06:28 14:05:11`). Si aucune photo géotaguée sous la main, tester la robustesse « sans GPS » :
```bash
core_extract_meta "/chemin/vers/image-sans-gps.png"
```
Expected: une ligne avec lat/lon vides (ex. `  2026:01:02 …` ou `  `), **sans crash**.

- [ ] **Step 4: Vérifier `core_geocode` (reverse) et `core_forward_geocode`**

Run:
```bash
source scripts/iso360-core.sh
LAT=46.8327 LON=-71.2445 DT="2026:06:28 14:05:11" core_geocode
echo "---"
core_forward_geocode "chute-montmorency"
```
Expected: (1) un JSON avec `"city": "quebec"`, un `"name"` non vide, un `"id"` slugifié, `"ym": "2026-06"` ; (2) une ligne `LAT LON` proche de `46.89 -71.15` (Chute Montmorency). *(Requiert le réseau ; Nominatim ~1 req/s.)*

- [ ] **Step 5: Refactorer `iso360.sh` pour `source` le cœur**

Dans `scripts/iso360.sh`, juste après le bloc de configuration (après la ligne `SEG_WIDTH=2016 …`, avant les helpers `b()/ok()/…`), ajouter :
```bash
# ─── Cœur partagé (géoloc, publie, câble, build, push) ───────────────────────
source "$(dirname "$0")/iso360-core.sh"
```

Puis remplacer les sections suivantes de `iso360.sh` par des appels au cœur :

**(a) Bloc GPS/géoloc (lignes ~135-223)** — remplacer tout le heredoc Python `GEO_JSON=$(…PY)` et l'extraction `GEO_NAME/GEO_CITY/…` par :
```bash
# ─── 3. GPS EXIF + géolocalisation (via le cœur) ─────────────────────────────
if [[ "$FILE_MODE" == "1" ]]; then FIRST="$SESSION"; else
  FIRST=$(find "$SESSION" -maxdepth 1 -iname 'PANO_*.JPG' ! -name '._*' | sort | head -1)
fi
read -r M_LAT M_LON M_DT < <(core_extract_meta "$FIRST")
[[ -n "$LAT" ]] && M_LAT="$LAT"
[[ -n "$LON" ]] && M_LON="$LON"
GEO_JSON=$(LAT="$M_LAT" LON="$M_LON" DT="$M_DT" NAME_OVR="$NAME" CITY_OVR="$CITY" ID_OVR="$ID" core_geocode)
GEO_NAME=$(echo "$GEO_JSON" | python3 -c 'import json,sys;print(json.load(sys.stdin)["name"])')
GEO_CITY=$(echo "$GEO_JSON" | python3 -c 'import json,sys;print(json.load(sys.stdin)["city"])')
GEO_ID=$(echo "$GEO_JSON" | python3 -c 'import json,sys;print(json.load(sys.stdin)["id"])')
GEO_YM=$(echo "$GEO_JSON" | python3 -c 'import json,sys;print(json.load(sys.stdin).get("ym") or "")')
GEO_DISPLAY=$(echo "$GEO_JSON" | python3 -c 'import json,sys;print(json.load(sys.stdin).get("display",""))')
[[ -n "$GEO_YM" ]] || GEO_YM=$(date +%Y-%m)
FILE="${GEO_ID}-${GEO_YM}.jpg"
ok "Lieu : $GEO_NAME  ($GEO_CITY)"
[[ -n "$GEO_DISPLAY" ]] && info "Adresse : $GEO_DISPLAY"
info "Fichier média : $FILE"
```

**(b) Publication + vérification (lignes ~232-243)** — remplacer par :
```bash
# ─── 4-5. Publication sur le tunnel + vérification (via le cœur) ──────────────
core_publish_verify "$WORK/pano.jpg" "$MEDIA_DIR" "$MEDIA_BASE_URL" "$FILE" \
  || die "Publication/vérification échouée (tunnel/Caddy arrêté ? cf handoff §3)"
```

**(c) Câblage (lignes ~245-279)** — remplacer par :
```bash
# ─── 6. Câblage dans labs360.ts (via le cœur) ────────────────────────────────
cd "$REPO"
GEO_JSON="$GEO_JSON" MEDIA_URL="$MEDIA_BASE_URL/$FILE" PTYPE='360' POSTER_URL='' \
  REPLACE="$REPLACE" WIRE_NOTE='Pano drone auto-publié par iso360' DATA="$DATA_FILE" \
  core_wire || die "Câblage labs360.ts échoué"
ok "labs360.ts câblé"
```

**(d) Build garde-fou (lignes ~281-287)** — remplacer par :
```bash
# ─── 7. Garde-fou : build avant de pousser (via le cœur) ─────────────────────
core_build_guard "$DATA_FILE" || die "Build échoué → rien poussé. Voir /tmp/iso360-build.log"
```

**(e) Commit + push (lignes ~289-312)** — remplacer par :
```bash
# ─── 8. Commit + push (via le cœur) ──────────────────────────────────────────
verb=$([[ -n "$REPLACE" ]] && echo "update" || echo "add")
core_commit_push "$DATA_FILE" "$verb" "$GEO_NAME" "$PUSH" "$LIVE_PAGE" "$FILE"
```

*(Note : les fonctions `b/ok/info/die` locales de iso360.sh restent utilisées ; le cœur logue via ses propres `core_*` sur stderr. Les deux coexistent.)*

- [ ] **Step 6: Vérification de non-régression — `iso360 --dry-run`**

Le `--dry-run` de iso360 s'arrête avant publication (ligne ~225) : il n'appelle donc pas encore le cœur pour publier, mais **doit** produire le même GEO_JSON qu'avant. Tester sur une session/fichier disponible :
```bash
./scripts/iso360.sh "/chemin/vers/pano-fini.jpg" --dry-run
# ou, si la carte SD est montée :
./scripts/iso360.sh "/Volumes/SD_Card/DCIM/PANORAMA/001_0218" --dry-run
```
Expected: le lieu géolocalisé s'affiche (nom + ville + adresse), un JSON est imprimé, un aperçu déposé sur le Bureau, **rien publié**. Le nom/ville doivent correspondre à ce qu'iso360 produisait avant (ex. Giffard/Beauport pour `001_0218`). Aucun crash bash.

- [ ] **Step 7: Vérifier que `bash -n` (syntaxe) passe sur les deux scripts**

Run:
```bash
bash -n scripts/iso360-core.sh && bash -n scripts/iso360.sh && echo "syntaxe OK"
```
Expected: `syntaxe OK`.

- [ ] **Step 8: Commit**

```bash
git add scripts/iso360-core.sh scripts/iso360.sh
git commit -m "refactor(iso360): extract shared core (geo/publish/wire/build/push)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4: `iso-ingest.sh` — l'ingesteur de la boîte de dépôt

**Files:**
- Create: `scripts/iso-ingest.sh`

**Interfaces:**
- Consumes : toutes les fonctions `core_*` de `scripts/iso360-core.sh` (Task 3).
- Produces : exécutable `iso-ingest.sh` qui traite chaque fichier de `inbox/`. Appelé par le LaunchAgent (Task 5). Options `--dry-run` (détecte + géolocalise, ne publie rien), `--no-push` (publie + câble + build + commit local, pas de push).

- [ ] **Step 1: Créer `scripts/iso-ingest.sh`**

Créer `scripts/iso-ingest.sh` :
```bash
#!/usr/bin/env bash
#
# iso-ingest — boîte de dépôt : un fichier déposé dans inbox/ devient un pin
# géolocalisé sur theo-picture.com/labs/360. Déclenché par un LaunchAgent
# (WatchPaths) ou lancé à la main. Mac Pro NAS headless → AUCUN popup :
# l'interaction se fait par les fichiers (inbox-corriger/) et le log.
#
# Usage : iso-ingest [--dry-run] [--no-push]
#
# Types détectés :  .mp4/.mov → video ;  image 2:1 → 360 ;  autre image → photo
# GPS : exiftool. Absent → nom de fichier ("lieu" ou "lat,lon"). Rien → quarantaine.

set -uo pipefail   # PAS -e : on gère les échecs par fichier sans tuer la boucle

REPO="$HOME/Desktop/Developer/iso-nord"
MEDIA_ROOT="/Volumes/SSD 1/iso-nord-media"
INBOX="$MEDIA_ROOT/inbox"
CORRIGER="$MEDIA_ROOT/inbox-corriger"
PUBLIES="$MEDIA_ROOT/inbox-publies"
LOGFILE="$MEDIA_ROOT/inbox.log"
LOCK="$MEDIA_ROOT/inbox.lock"

export MEDIA_BASE_URL="https://media.theo-picture.com"   # utilisé par core_commit_push
LIVE_PAGE="https://theo-picture.com/labs/360"
DATA_FILE="$REPO/src/data/labs360.ts"

DRY=0; PUSH=1
for a in "$@"; do
  case "$a" in
    --dry-run) DRY=1;;
    --no-push) PUSH=0;;
    *) echo "Option inconnue : $a" >&2; exit 2;;
  esac
done

source "$(dirname "$0")/iso360-core.sh"

log(){ printf '%s %s\n' "$(date '+%Y-%m-%d %H:%M:%S')" "$*" | tee -a "$LOGFILE" >&2; }

# ─── Verrou (traitement séquentiel) ──────────────────────────────────────────
[[ -d "$MEDIA_ROOT" ]] || { echo "SSD non monté : $MEDIA_ROOT" >&2; exit 0; }
mkdir -p "$INBOX" "$CORRIGER" "$PUBLIES" "$MEDIA_ROOT/panoramas" "$MEDIA_ROOT/videos" "$MEDIA_ROOT/photos"
if ! mkdir "$LOCK" 2>/dev/null; then
  log "Déjà en cours (lock présent) — sortie."
  exit 0
fi
trap 'rmdir "$LOCK" 2>/dev/null' EXIT

# ─── Quarantaine ─────────────────────────────────────────────────────────────
quarantine(){  # <fichier> <raison>
  local f="$1" reason="$2" base; base=$(basename "$f")
  mv "$f" "$CORRIGER/$base" 2>/dev/null
  printf '%s\n\nRenomme ce fichier avec le lieu puis redépose-le dans inbox/.\nExemples : chute-montmorency.jpg  (nom, géocodé)  ou  46.89,-71.15.jpg  (coordonnées).\n' \
    "$reason" > "$CORRIGER/${base%.*}.txt"
  log "QUARANTAINE : $base — $reason"
}

# ─── Détection du type ───────────────────────────────────────────────────────
detect_type(){  # <fichier> → echo video|360|photo|unknown
  local f="$1" ext; ext=$(echo "${f##*.}" | tr '[:upper:]' '[:lower:]')
  case "$ext" in
    mp4|mov|m4v) echo video; return;;
    jpg|jpeg|png|heic|heif|tif|tiff)
      local w h; w=$(sips -g pixelWidth "$f" 2>/dev/null | awk '/pixelWidth/{print $2}')
      h=$(sips -g pixelHeight "$f" 2>/dev/null | awk '/pixelHeight/{print $2}')
      if [[ -n "$w" && -n "$h" && "$h" -gt 0 ]] && awk "BEGIN{exit !($w/$h>1.8 && $w/$h<2.2)}"; then
        echo 360
      else
        echo photo
      fi;;
    *) echo unknown;;
  esac
}

# ─── Traitement d'UN fichier ─────────────────────────────────────────────────
process(){
  local f="$1" base; base=$(basename "$f")
  local ptype; ptype=$(detect_type "$f")
  [[ "$ptype" == "unknown" ]] && { quarantine "$f" "Type non reconnu (extension inattendue)."; return; }
  log "Traitement : $base (type=$ptype)"

  # GPS + date via exiftool
  local lat lon dt; read -r lat lon dt < <(core_extract_meta "$f")

  # Repli : nom de fichier (coordonnées ou lieu à géocoder)
  local name_ovr=""
  if [[ -z "$lat" || -z "$lon" ]]; then
    local stem="${base%.*}"
    if [[ "$stem" =~ ^(-?[0-9]+\.[0-9]+),(-?[0-9]+\.[0-9]+)$ ]]; then
      lat="${BASH_REMATCH[1]}"; lon="${BASH_REMATCH[2]}"
      log "GPS depuis le nom de fichier : $lat,$lon"
    else
      read -r flat flon < <(core_forward_geocode "$stem")
      if [[ -n "${flat:-}" && -n "${flon:-}" ]]; then
        lat="$flat"; lon="$flon"; name_ovr="${stem//-/ }"
        log "Lieu géocodé depuis le nom : $stem → $lat,$lon"
      fi
    fi
  fi
  if [[ -z "$lat" || -z "$lon" ]]; then
    quarantine "$f" "GPS introuvable (ni dans les métadonnées, ni via le nom de fichier)."
    return
  fi

  # Géolocalisation (nom + ville + id + ym)
  local geo; geo=$(LAT="$lat" LON="$lon" DT="$dt" NAME_OVR="$name_ovr" core_geocode)
  local gname gid gym gcity
  gname=$(echo "$geo" | python3 -c 'import json,sys;print(json.load(sys.stdin)["name"])')
  gid=$(echo   "$geo" | python3 -c 'import json,sys;print(json.load(sys.stdin)["id"])')
  gym=$(echo   "$geo" | python3 -c 'import json,sys;print(json.load(sys.stdin).get("ym") or "")')
  gcity=$(echo "$geo" | python3 -c 'import json,sys;print(json.load(sys.stdin)["city"])')
  [[ -n "$gym" ]] || gym=$(date +%Y-%m)

  # Préparer le média + choisir dossier/sous-domaine + poster éventuel
  local work destdir filename ext poster_url="" poster_local=""
  work="$(mktemp -d "${TMPDIR:-/tmp}/iso-ingest.XXXXXX")"
  case "$ptype" in
    360)
      destdir="$MEDIA_ROOT/panoramas"; filename="${gid}-${gym}.jpg"
      sips -s format jpeg -s formatOptions 88 "$f" --out "$work/out.jpg" >/dev/null 2>&1;;
    photo)
      destdir="$MEDIA_ROOT/photos"; filename="${gid}-${gym}.jpg"
      sips -s format jpeg -s formatOptions 88 -Z 2560 "$f" --out "$work/out.jpg" >/dev/null 2>&1;;
    video)
      ext=$(echo "${f##*.}" | tr '[:upper:]' '[:lower:]')
      destdir="$MEDIA_ROOT/videos"; filename="${gid}-${gym}.${ext}"
      cp "$f" "$work/out.${ext}"
      # Poster = frame à ~1 s
      if ffmpeg -y -ss 1 -i "$f" -frames:v 1 -q:v 3 "$work/poster.jpg" >/dev/null 2>&1 \
         && [[ -s "$work/poster.jpg" ]]; then
        poster_local="$work/poster.jpg"
      fi;;
  esac

  local outfile; outfile=$(ls "$work"/out.* 2>/dev/null | head -1)
  if [[ -z "$outfile" || ! -s "$outfile" ]]; then
    rm -rf "$work"; quarantine "$f" "Préparation du média échouée (fichier illisible ?)."; return
  fi

  if [[ "$DRY" == "1" ]]; then
    log "DRY-RUN : $base → type=$ptype lieu=«$gname» ($gcity) fichier=$filename lat=$lat lon=$lon poster=$([[ -n "$poster_local" ]] && echo oui || echo non)"
    rm -rf "$work"; return
  fi

  # Publier le média (+ poster vidéo)
  cd "$REPO"
  if ! core_publish_verify "$outfile" "$destdir" "$MEDIA_BASE_URL/$(basename "$destdir")" "$filename"; then
    rm -rf "$work"; log "ÉCHEC publication : $base laissé dans inbox/ (tunnel/Caddy ?)"; return
  fi
  if [[ -n "$poster_local" ]]; then
    local poster_file="${gid}-${gym}-poster.jpg"
    if core_publish_verify "$poster_local" "$MEDIA_ROOT/photos" "$MEDIA_BASE_URL/photos" "$poster_file"; then
      poster_url="$MEDIA_BASE_URL/photos/$poster_file"
    fi
  fi
  rm -rf "$work"

  # Câbler labs360.ts
  local media_url="$MEDIA_BASE_URL/$(basename "$destdir")/$filename"
  if ! GEO_JSON="$geo" MEDIA_URL="$media_url" PTYPE="$ptype" POSTER_URL="$poster_url" \
       REPLACE="" WIRE_NOTE="Auto-publié par iso-ingest" DATA="$DATA_FILE" core_wire; then
    log "ÉCHEC câblage : $base (marqueur iso360:insert absent ?)"; return
  fi

  # Build garde-fou
  if ! core_build_guard "$DATA_FILE"; then
    log "ÉCHEC build : labs360.ts restauré, $base laissé dans inbox/"; return
  fi

  # Commit + push
  core_commit_push "$DATA_FILE" "add" "$gname" "$PUSH" "$LIVE_PAGE" "$filename"

  # Archiver l'original
  mv "$f" "$PUBLIES/$base" 2>/dev/null
  log "PUBLIÉ : $base → pin «$gname» ($media_url)"
}

# ─── Boucle : chaque fichier de premier niveau de inbox/ ─────────────────────
shopt -s nullglob
found=0
for f in "$INBOX"/*; do
  [[ -f "$f" ]] || continue
  b=$(basename "$f")
  [[ "$b" == .* ]] && continue
  # Débounce : taille stable = copie réseau terminée
  s1=$(stat -f%z "$f" 2>/dev/null || echo 0); sleep 2
  s2=$(stat -f%z "$f" 2>/dev/null || echo 0)
  [[ "$s1" == "$s2" && "$s1" != "0" ]] || { log "En cours de copie, ignoré ce tour : $b"; continue; }
  found=1
  process "$f"
done
[[ "$found" == "0" ]] && log "Aucun fichier à traiter."
exit 0
```

- [ ] **Step 2: Rendre le script exécutable + vérifier la syntaxe**

Run:
```bash
chmod +x scripts/iso-ingest.sh
bash -n scripts/iso-ingest.sh && echo "syntaxe OK"
```
Expected: `syntaxe OK`.

- [ ] **Step 3: Vérifier `detect_type` sur des fixtures**

Créer des fixtures et tester la détection (isoler la fonction en la `source`ant via un sous-shell) :
```bash
mkdir -p /tmp/ingest-fix
# image 2:1 (pano) et image carrée (photo) via sips sur une image existante du repo
sips -z 1500 3000 public/assets/portfolio/chute-automne.jpeg --out /tmp/ingest-fix/pano.jpg >/dev/null
sips -z 1200 1200 public/assets/portfolio/chute-automne.jpeg --out /tmp/ingest-fix/carre.jpg >/dev/null
bash -c '
  source scripts/iso360-core.sh
  # extraire detect_type par eval du bloc : plus simple, appeler le script en mode fonction
'
# Vérif directe via un mini-harnais :
python3 - <<'"'"'PY'"'"'
import subprocess
PY
```

Plus simplement, tester la logique de ratio en ligne :
```bash
for img in /tmp/ingest-fix/pano.jpg /tmp/ingest-fix/carre.jpg; do
  w=$(sips -g pixelWidth "$img" | awk '/pixelWidth/{print $2}')
  h=$(sips -g pixelHeight "$img" | awk '/pixelHeight/{print $2}')
  awk "BEGIN{r=$w/$h; print \"$img\", (r>1.8&&r<2.2)?\"360\":\"photo\"}"
done
```
Expected: `.../pano.jpg 360` et `.../carre.jpg photo`.

- [ ] **Step 4: Test `--dry-run` de bout en bout (sans rien publier)**

Déposer des fixtures dans une inbox de test et lancer en dry-run. Le dry-run n'écrit rien sur le tunnel ni le repo.
```bash
# Photo géotaguée (si dispo) → doit résoudre un lieu ; sinon nommer avec un lieu
cp /tmp/ingest-fix/carre.jpg "/tmp/ingest-fix/chute-montmorency.jpg"
# Simuler l'inbox : pointer temporairement le script via une copie modifiée n'est pas
# nécessaire — on teste la résolution nom→GPS directement :
source scripts/iso360-core.sh
read -r la lo < <(core_forward_geocode "chute-montmorency")
echo "résolu: $la $lo"
LAT="$la" LON="$lo" DT="" NAME_OVR="chute montmorency" core_geocode
```
Expected: `résolu:` avec des coordonnées près de `46.89 -71.15`, puis un JSON `city: quebec`, `name` non vide. *(Le chemin complet inbox→dry-run est validé en Task 5 avec le SSD monté ; ici on valide la logique de résolution hors SSD.)*

- [ ] **Step 5: Test intégré `--no-push` (si le SSD + tunnel sont dispos)**

Si `/Volumes/SSD 1/iso-nord-media` est monté et le tunnel actif : déposer une vraie photo géotaguée dans `inbox/`, lancer sans push, vérifier l'insertion + build, puis **annuler** le commit local de test.
```bash
cp "/chemin/vers/photo-quebec-geotaguee.jpg" "/Volumes/SSD 1/iso-nord-media/inbox/"
./scripts/iso-ingest.sh --no-push
git log --oneline -1            # doit montrer un commit "add '<lieu>'"
grep -c "iso360:insert" src/data/labs360.ts   # toujours 1
tail -5 "/Volumes/SSD 1/iso-nord-media/inbox.log"
# Annuler le test :
git reset --hard HEAD~1
```
Expected: le log montre `PUBLIÉ : … → pin «<lieu>»`, un commit local a été créé, le build est passé (10 pages). Après `git reset --hard HEAD~1`, `labs360.ts` revient à l'état d'avant. *(Si le SSD/tunnel ne sont pas dispos sur la machine de dev, sauter ce step — il sera exécuté sur le NAS.)*

- [ ] **Step 6: Commit**

```bash
git add scripts/iso-ingest.sh
git commit -m "feat(labs360): iso-ingest — boîte de dépôt auto (type + GPS + poster)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: LaunchAgent `WatchPaths` + documentation handoff

**Files:**
- Create: `launchd/com.iso-nord.inbox.plist`
- Modify: `docs/HANDOFF-codex.md` (nouvelle section « boîte de dépôt »)

**Interfaces:**
- Consumes : `scripts/iso-ingest.sh` (Task 4).
- Produces : un LaunchAgent installable qui lance `iso-ingest.sh` à chaque changement de `inbox/`.

- [ ] **Step 1: Créer le plist du LaunchAgent**

Créer `launchd/com.iso-nord.inbox.plist` (⚠️ le chemin absolu du script et le PATH incluant Homebrew pour `exiftool`/`ffmpeg` ; adapter `USERNAME` à l'install) :
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.iso-nord.inbox</string>

  <key>ProgramArguments</key>
  <array>
    <string>/Users/theodorebeaupre/Desktop/Developer/iso-nord/scripts/iso-ingest.sh</string>
  </array>

  <!-- Déclenché quand le contenu de inbox/ change (copie SMB terminée → nouvel event). -->
  <key>WatchPaths</key>
  <array>
    <string>/Volumes/SSD 1/iso-nord-media/inbox</string>
  </array>

  <!-- Homebrew (exiftool, ffmpeg) + git/node dans le PATH du daemon. -->
  <key>EnvironmentVariables</key>
  <dict>
    <key>PATH</key>
    <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
  </dict>

  <key>StandardOutPath</key>
  <string>/Volumes/SSD 1/iso-nord-media/inbox-launchd.out.log</string>
  <key>StandardErrorPath</key>
  <string>/Volumes/SSD 1/iso-nord-media/inbox-launchd.err.log</string>

  <key>RunAtLoad</key>
  <false/>
</dict>
</plist>
```

- [ ] **Step 2: Valider la syntaxe du plist**

Run:
```bash
plutil -lint launchd/com.iso-nord.inbox.plist
```
Expected: `launchd/com.iso-nord.inbox.plist: OK`.

- [ ] **Step 3: Installer + charger le LaunchAgent (sur le NAS)**

Run:
```bash
cp launchd/com.iso-nord.inbox.plist ~/Library/LaunchAgents/
launchctl unload ~/Library/LaunchAgents/com.iso-nord.inbox.plist 2>/dev/null
launchctl load  ~/Library/LaunchAgents/com.iso-nord.inbox.plist
launchctl list | grep iso-nord
```
Expected: une ligne `com.iso-nord.inbox` (chargé). *(À exécuter sur le Mac Pro NAS ; le PATH Homebrew doit correspondre — `/opt/homebrew/bin` sur Apple Silicon, `/usr/local/bin` sur Intel.)*

- [ ] **Step 4: Test bout-en-bout réel (sur le NAS, avec push)**

Déposer une vraie photo/clip géotagué de Québec dans le partage `inbox/`, attendre, vérifier la mise en ligne.
```bash
# depuis un autre appareil : glisser un fichier dans le partage SMB → inbox/
# puis, sur le NAS :
tail -f "/Volumes/SSD 1/iso-nord-media/inbox.log"
```
Expected: en quelques secondes, le log montre `Traitement`, puis `PUBLIÉ : … → pin «<lieu>»`, l'original est passé dans `inbox-publies/`, et le pin apparaît sur `https://theo-picture.com/labs/360` (jusqu'à ~3 min de propagation Vercel). Un fichier sans GPS et mal nommé atterrit dans `inbox-corriger/` avec un `.txt`.

- [ ] **Step 5: Documenter dans le handoff**

Dans `docs/HANDOFF-codex.md`, ajouter une section (après §5 « Stitching ») :
```markdown
## 5bis. Boîte de dépôt automatique (iso-ingest)

Déposer un fichier dans le partage SMB `/Volumes/SSD 1/iso-nord-media/inbox/`
publie automatiquement un pin sur `/labs/360`. Sur le Mac Pro NAS **headless** :
aucun popup — tout passe par les fichiers et le log.

| Élément | Chemin |
|---|---|
| Dossier de dépôt (surveillé) | `/Volumes/SSD 1/iso-nord-media/inbox/` |
| Quarantaine (GPS/type manquant) | `/Volumes/SSD 1/iso-nord-media/inbox-corriger/` |
| Archive (originaux publiés) | `/Volumes/SSD 1/iso-nord-media/inbox-publies/` |
| Journal | `/Volumes/SSD 1/iso-nord-media/inbox.log` |
| Script | `scripts/iso-ingest.sh` (cœur partagé : `scripts/iso360-core.sh`) |
| LaunchAgent | `~/Library/LaunchAgents/com.iso-nord.inbox.plist` |

**Types :** `.mp4/.mov` → clip (poster ffmpeg) ; image ~2:1 → pano 360 ; autre
image → photo (lightbox). **GPS** via exiftool ; si absent, le **nom du fichier**
sert de repli (`chute-montmorency.jpg` géocodé, ou `46.89,-71.15.jpg` en coordonnées).
Sans lieu fiable → `inbox-corriger/` + un `.txt` explicatif (renommer + redéposer).

**Dépendances :** `brew install exiftool ffmpeg`. **Ne stitche PAS** les 35 segments
DJI bruts (ça reste la commande manuelle `iso360`) — l'inbox n'accepte que des
fichiers finis.

**(Ré)installer le watcher :**
    cp launchd/com.iso-nord.inbox.plist ~/Library/LaunchAgents/
    launchctl unload ~/Library/LaunchAgents/com.iso-nord.inbox.plist 2>/dev/null
    launchctl load  ~/Library/LaunchAgents/com.iso-nord.inbox.plist

**Dépannage :** logs dans `inbox.log` (+ `inbox-launchd.err.log`). Rien ne se passe →
`launchctl list | grep iso-nord`. GPS manquant → voir `inbox-corriger/`. URL 200 KO →
tunnel/Caddy (cf §3). PATH Homebrew du plist : `/opt/homebrew/bin` (Apple Silicon)
ou `/usr/local/bin` (Intel).
```

- [ ] **Step 6: Commit**

```bash
git add launchd/com.iso-nord.inbox.plist docs/HANDOFF-codex.md
git commit -m "feat(labs360): LaunchAgent WatchPaths + doc boîte de dépôt

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review (fait à l'écriture — pour info)

- **Couverture spec §4 (fichiers)** : core (T3), iso-ingest (T4), plist (T5), labs360.ts/js/astro/ui (T2), handoff (T5). ✓
- **Spec §5 (flux)** : débounce (T4 boucle), détection (T4 detect_type), GPS exiftool + repli nom (T4 process), média/poster (T4), publish/verify/wire/build/push (core, T3+T4), archive/quarantaine/lock/log (T4). ✓
- **Spec §6 (type photo)** : T2 couvre data + i18n + astro + js + glyph. ✓
- **Spec §7 (erreurs)** : quarantaine, lock, rollback build, laisser en inbox sur échec publication — tous dans T4. ✓
- **Spec §3 (deps)** : T1. ✓
- **Cohérence des noms** : `core_wire` consomme `PTYPE/POSTER_URL/WIRE_NOTE` — produits identiques dans T3 (def) et T4/iso360 (appel). `core_extract_meta` renvoie `LAT LON DT` — lu par `read -r` partout. `MEDIA_BASE_URL` exporté par iso-ingest, utilisé par `core_commit_push`. ✓
- **Isolement WatchPaths** : quarantaine/archive/log tous frères de `inbox/`. ✓
