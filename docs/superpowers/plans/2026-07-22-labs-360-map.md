# Labs « Québec en 360 » (labs-360-map) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter `/labs/360` (FR) et `/en/labs/360` (EN) — carte stylisée de lieux Québec/Montréal dont les pins ouvrent un viewer 360 Pannellum ou un clip vidéo en modal.

**Architecture:** Une page composant bilingue `Labs360.astro` (pattern `Labs.astro`), données dans `src/data/labs360.ts` (seul fichier que Théo éditera), interactions dans `src/scripts/labs360.js`. Pannellum est chargé en dynamic import à la première ouverture d'un pin 360.

**Tech Stack:** Astro 6, Tailwind v4 (tokens CSS custom de `global.css`), GSAP + ScrollTrigger, Lenis, Pannellum 2.5.7.

## Global Constraints

- Repo : `~/Desktop/Developer/iso-nord` — spec : `docs/superpowers/specs/2026-07-22-labs-360-map-design.md`.
- Palette sombre du site : fond `--bg` #141318, accent `--accent` (#c8ff00), labels `--font-label` (Barlow Condensed).
- Médias swappables : `MEDIA_BASE = import.meta.env.PUBLIC_MEDIA_BASE ?? '/assets/labs360'` ; chemins commençant par `/` ou `http` passent tels quels.
- Pas de suite de tests dans le repo → chaque tâche se vérifie par `npm run build` (doit passer de 8 à 10 pages une fois les routes créées) puis inspection navigateur en fin de plan.
- Commentaires de code en français, dans la voix du repo.
- `credit` par défaut : `'[contributeur]'` (Théo assignera les vrais noms).
- A11y : pins = `<button>` ≥44 px, modal `role="dialog"` + focus piégé + Échap, `prefers-reduced-motion` respecté partout.

---

### Task 1: Dépendance Pannellum + données + i18n + registre de routes

**Files:**
- Modify: `package.json` (via `npm install pannellum`)
- Create: `src/data/labs360.ts`
- Modify: `src/i18n/utils.ts` (ajout d'une entrée dans `PAGES`)
- Modify: `src/i18n/ui.ts` (section `labs360` dans `fr` et `en`)

**Interfaces:**
- Produces: `PLACES: Labs360Place[]`, `MEDIA_BASE`, `mediaUrl(path)`, type `Labs360Place { id, city: 'quebec'|'montreal', type: '360'|'video', name, desc: {fr,en}, credit, x, y, media, poster? }` ; clé de page `'labs360'` utilisable par `pathFor`/`alternates`/`LangSwitch` ; `ui[lang].labs360` avec les clés utilisées par la Task 3.

- [ ] **Step 1: Installer Pannellum**

Run: `cd ~/Desktop/Developer/iso-nord && npm install pannellum`
Expected: `added 1 package`, `pannellum@2.5.7` dans package.json.

- [ ] **Step 2: Créer `src/data/labs360.ts`**

```ts
/**
 * Labs « Québec en 360 » — données des lieux.
 *
 * C'EST LE FICHIER À ÉDITER pour ajouter/retirer des lieux ou brancher les
 * vrais médias. Les chemins relatifs sont préfixés par MEDIA_BASE (variable
 * d'environnement PUBLIC_MEDIA_BASE → bucket Cloudflare R2 en production) ;
 * les chemins absolus (`/…` ou `https://…`) passent tels quels.
 *
 * Panorama 360 : image équirectangulaire 2:1 (drone). Vidéo : clip court MP4.
 * `x`/`y` : position du pin sur la carte stylisée, en % (0–100) depuis le
 * coin haut-gauche — pas une vraie projection, juste une disposition plausible.
 * `credit` : contributeur de la capture ('[contributeur]' tant que non confirmé).
 */

export type City = 'quebec' | 'montreal';
export type PlaceType = '360' | 'video';

export interface Labs360Place {
  id: string;
  city: City;
  type: PlaceType;
  name: string;                       // nom propre — identique fr/en
  desc: { fr: string; en: string };
  credit: string;
  x: number;                          // % depuis la gauche
  y: number;                          // % depuis le haut
  media: string;
  poster?: string;                    // affiche (vidéo surtout)
}

/** Base des médias — surchargée par PUBLIC_MEDIA_BASE (Cloudflare R2). */
export const MEDIA_BASE: string =
  import.meta.env.PUBLIC_MEDIA_BASE ?? '/assets/labs360';

/** Résout un chemin média : relatif → MEDIA_BASE, absolu → inchangé. */
export const mediaUrl = (path: string): string =>
  !path || path.startsWith('http') || path.startsWith('/')
    ? path
    : `${MEDIA_BASE}/${path}`;

export const PLACES: Labs360Place[] = [
  // ── Québec ────────────────────────────────────────────────────────────────
  {
    id: 'vieux-quebec',
    city: 'quebec',
    type: '360',
    name: 'Vieux-Québec',
    desc: {
      fr: "Les toits du Vieux-Québec et le Château Frontenac, captés à l'aube.",
      en: 'The rooftops of Old Québec and the Château Frontenac, captured at dawn.',
    },
    credit: '[contributeur]',
    x: 62, y: 55,
    media: 'pano-vieux-quebec.png',
  },
  {
    id: 'chute-montmorency',
    city: 'quebec',
    type: '360',
    name: 'Chute Montmorency',
    desc: {
      fr: 'La chute et son embrun, 83 mètres au-dessus du Saint-Laurent.',
      en: 'The falls and their mist, 83 metres above the St. Lawrence.',
    },
    credit: '[contributeur]',
    x: 78, y: 30,
    media: 'pano-chute-montmorency.png',
  },
  {
    id: 'ile-orleans',
    city: 'quebec',
    type: 'video',
    name: "Île d'Orléans",
    desc: {
      fr: "Survol des rives et des vergers de l'île, au fil des saisons.",
      en: "Flying over the island's shores and orchards, season by season.",
    },
    credit: '[contributeur]',
    x: 85, y: 48,
    media: '/assets/hero-camera.mp4',
    poster: '/assets/portfolio/chute-automne.jpeg',
  },
  {
    id: 'maizerets',
    city: 'quebec',
    type: '360',
    name: 'Domaine de Maizerets',
    desc: {
      fr: 'Le domaine et son arboretum, vus du ciel en fin de journée.',
      en: 'The estate and its arboretum, seen from above at dusk.',
    },
    credit: '[contributeur]',
    x: 58, y: 38,
    media: 'pano-maizerets.png',
  },

  // ── Montréal ──────────────────────────────────────────────────────────────
  {
    id: 'vieux-port',
    city: 'montreal',
    type: '360',
    name: 'Vieux-Port de Montréal',
    desc: {
      fr: 'Les quais, la grande roue et le fleuve — panorama complet du Vieux-Port.',
      en: 'The docks, the Ferris wheel and the river — a full panorama of the Old Port.',
    },
    credit: '[contributeur]',
    x: 55, y: 62,
    media: 'pano-vieux-port.png',
  },
  {
    id: 'mont-royal',
    city: 'montreal',
    type: 'video',
    name: 'Mont-Royal',
    desc: {
      fr: 'La montagne et le belvédère Kondiaronk face au centre-ville.',
      en: 'The mountain and the Kondiaronk lookout facing downtown.',
    },
    credit: '[contributeur]',
    x: 42, y: 45,
    media: '/assets/hero-camera.mp4',
    poster: '/assets/portfolio/skyline-quebec.jpeg',
  },
  {
    id: 'centre-ville',
    city: 'montreal',
    type: 'video',
    name: 'Centre-ville de Montréal',
    desc: {
      fr: 'Les tours du centre-ville à l’heure bleue, en survol cinématique.',
      en: 'Downtown towers at blue hour, in a cinematic flyover.',
    },
    credit: '[contributeur]',
    x: 50, y: 52,
    media: '/assets/hero-camera.mp4',
    poster: '/assets/portfolio/ville-heure-bleue.jpeg',
  },
];
```

- [ ] **Step 3: Enregistrer la route dans `src/i18n/utils.ts`**

Dans le bloc `PAGES`, après la ligne `labs:`, ajouter :

```ts
  labs360:   { fr: '/labs/360',        en: '/en/labs/360' },
```

- [ ] **Step 4: Ajouter la section `labs360` dans `src/i18n/ui.ts`**

Dans `fr`, après le bloc `labs: { … },` :

```ts
    labs360: {
      title: 'ISO Nord — Québec en 360',
      description: "Québec en 360 — lieux iconiques de Québec et Montréal captés en drone : panoramas 360° explorables et clips aériens, par ISO Nord Labs.",
      ogDescription: 'Lieux iconiques de Québec et Montréal — panoramas drone 360° explorables et clips aériens.',
      eyebrow: 'ISO Nord — Labs · 360',
      titleLines: ['Québec', 'en 360.'],
      lede: "Des lieux iconiques captés en drone — choisissez une ville, touchez un repère, explorez le panorama ou lancez le clip.",
      cities: { quebec: 'Québec', montreal: 'Montréal' },
      cityAria: 'Choisir la ville',
      mapAria: 'Carte des lieux',
      legendAria: 'Liste des lieux',
      badge360: '360°',
      badgeVideo: 'Clip',
      dragHint: 'Glissez pour explorer',
      mediaSoon: 'Média à venir — captation en cours.',
      creditPrefix: 'Capté par',
      close: 'Fermer',
      backToLabs: 'Labs',
      footerBack: 'Retour au studio',
    },
```

Dans `en`, au même endroit :

```ts
    labs360: {
      title: 'ISO Nord — Québec in 360',
      description: 'Québec in 360 — iconic places in Québec City and Montréal captured by drone: explorable 360° panoramas and aerial clips, by ISO Nord Labs.',
      ogDescription: 'Iconic places in Québec City and Montréal — explorable 360° drone panoramas and aerial clips.',
      eyebrow: 'ISO Nord — Labs · 360',
      titleLines: ['Québec', 'in 360.'],
      lede: 'Iconic places captured by drone — pick a city, tap a marker, explore the panorama or play the clip.',
      cities: { quebec: 'Québec City', montreal: 'Montréal' },
      cityAria: 'Choose a city',
      mapAria: 'Map of places',
      legendAria: 'List of places',
      badge360: '360°',
      badgeVideo: 'Clip',
      dragHint: 'Drag to explore',
      mediaSoon: 'Media coming soon — capture in progress.',
      creditPrefix: 'Captured by',
      close: 'Close',
      backToLabs: 'Labs',
      footerBack: 'Back to the studio',
    },
```

- [ ] **Step 5: Vérifier le build**

Run: `npm run build`
Expected: `8 page(s) built`, aucune erreur TS.

- [ ] **Step 6: Commit**

```bash
git add package.json package-lock.json src/data/labs360.ts src/i18n/ui.ts src/i18n/utils.ts
git commit -m "feat(labs360): data model, i18n strings and route registry"
```

---

### Task 2: Placeholders médias (panoramas équirectangulaires synthétiques)

**Files:**
- Create: `public/assets/labs360/pano-vieux-quebec.png`, `pano-chute-montmorency.png`, `pano-maizerets.png`, `pano-vieux-port.png` (générés — non écrits à la main)

**Interfaces:**
- Consumes: noms de fichiers `media` de la Task 1.
- Produces: 4 PNG équirectangulaires 2:1 (1024×512) — dégradé ciel/sol + méridiens + nom du lieu, pour que le drag Pannellum soit visible en démo.

- [ ] **Step 1: Générer les 4 panoramas dans le scratchpad puis les copier**

Écrire `make_panos.py` dans le scratchpad de session (générateur jetable — PAS commité) puis l'exécuter. PIL si dispo, sinon écriture PNG brute via zlib :

```python
#!/usr/bin/env python3
"""Génère des panoramas équirectangulaires placeholder (1024x512, 2:1)."""
import math, os, struct, zlib

W, H = 1024, 512
OUT = os.path.expanduser('~/Desktop/Developer/iso-nord/public/assets/labs360')
os.makedirs(OUT, exist_ok=True)

PANOS = {
    'pano-vieux-quebec': (24, 26, 38),       # bleu nuit
    'pano-chute-montmorency': (20, 32, 34),  # sarcelle sombre
    'pano-maizerets': (22, 30, 24),          # vert sombre
    'pano-vieux-port': (32, 24, 36),         # violet sombre
}
LIME = (200, 255, 0)

def px(name, base):
    rows = []
    for y in range(H):
        t = y / H  # 0 = zénith, 1 = nadir
        row = bytearray()
        for x in range(W):
            # dégradé vertical ciel → horizon → sol
            if t < 0.55:
                k = t / 0.55
                c = [int(b * (0.35 + 0.65 * k) + 40 * k) for b in base]
            else:
                k = (t - 0.55) / 0.45
                c = [int(b * (1 - 0.6 * k)) for b in base]
            # méridiens tous les 45° + équateur : repères de drag
            lon = x / W * 360
            if min(lon % 45, 45 - lon % 45) < 0.25 or abs(t - 0.55) < 0.004:
                c = [min(255, v + 60) for v in c]
            # bande lime au nord (lon 0) — point de départ visible
            if lon < 1.2 and 0.45 < t < 0.55:
                c = list(LIME)
            row += bytes(c)
        rows.append(bytes(row))
    return rows

def write_png(path, rows):
    def chunk(tag, data):
        return (struct.pack('>I', len(data)) + tag + data
                + struct.pack('>I', zlib.crc32(tag + data)))
    raw = b''.join(b'\x00' + r for r in rows)
    png = (b'\x89PNG\r\n\x1a\n'
           + chunk(b'IHDR', struct.pack('>IIBBBBB', W, H, 8, 2, 0, 0, 0))
           + chunk(b'IDAT', zlib.compress(raw, 6))
           + chunk(b'IEND', b''))
    open(path, 'wb').write(png)

for name, base in PANOS.items():
    write_png(os.path.join(OUT, f'{name}.png'), px(name, base))
    print('ok', name)
```

Run: `python3 make_panos.py`
Expected: `ok pano-…` ×4 ; `ls public/assets/labs360` montre 4 PNG (~50–150 KB chacun).

- [ ] **Step 2: Commit**

```bash
git add public/assets/labs360
git commit -m "feat(labs360): synthetic equirectangular placeholder panoramas"
```

---

### Task 3: Routes + composant de page `Labs360.astro` (markup + styles, sans JS)

**Files:**
- Create: `src/pages/labs/360.astro`
- Create: `src/pages/en/labs/360.astro`
- Create: `src/components/pages/Labs360.astro`

**Interfaces:**
- Consumes: `PLACES`, `mediaUrl`, types de `../data/labs360` ; `ui[lang].labs360` ; `pathFor`, `alternates`, `SITE` ; `InstrumentHud`, `LangSwitch`.
- Produces: DOM contractuel pour la Task 4 — `[data-city-btn]`, `.l360-pins[data-city]`, `.l360-pin[data-place-id]`, `[data-legend-city]`, `#l360-modal`, `#l360-media`, `#l360-title`, `.l360-modal__desc`, `.l360-modal__credit`, `.l360-modal__hint`, `.l360-modal__fallback`, `[data-modal-close]`, `#l360-data` (JSON `{ lang, dragHint, mediaSoon, places: [{id,type,name,desc,credit,media,poster}] }`).

- [ ] **Step 1: Créer les deux wrappers de route**

`src/pages/labs/360.astro` :
```astro
---
import Labs360 from '../../components/pages/Labs360.astro';
---
<Labs360 lang="fr" />
```

`src/pages/en/labs/360.astro` :
```astro
---
import Labs360 from '../../../components/pages/Labs360.astro';
---
<Labs360 lang="en" />
```

- [ ] **Step 2: Créer `src/components/pages/Labs360.astro`**

Page sombre (tokens globaux), même squelette head/nav/footer que `Labs.astro`. Contenu complet :

```astro
---
/**
 * Labs — « Québec en 360 » (labs-360-map).
 *
 * Carte stylisée (pas de vraie API carte) : grille SVG subtile, pins en
 * coordonnées %, sélecteur Québec/Montréal. Un pin ouvre le modal viewer —
 * panorama équirectangulaire (Pannellum, chargé lazy par labs360.js) ou clip
 * vidéo. Les données vivent dans src/data/labs360.ts.
 */
import '../../styles/global.css';
import InstrumentHud from '../InstrumentHud.astro';
import LangSwitch from '../LangSwitch.astro';
import { ui, type Lang } from '../../i18n/ui';
import { pathFor, alternates, SITE } from '../../i18n/utils';
import { PLACES, mediaUrl, type City } from '../../data/labs360';

interface Props { lang: Lang }
const { lang } = Astro.props;
const c = ui[lang].common;
const l = ui[lang].labs360;

const canonical = SITE + pathFor('labs360', lang);
const alts = alternates('labs360');
const ogLocale = lang === 'fr' ? 'fr_CA' : 'en_CA';
const navAria = lang === 'fr' ? 'Navigation principale' : 'Main navigation';
const pad = (n: number) => String(n).padStart(2, '0');

const CITIES: City[] = ['quebec', 'montreal'];
const byCity = (city: City) => PLACES.filter((p) => p.city === city);

/* Données runtime pour labs360.js — médias résolus, description de la langue. */
const runtime = {
  lang,
  dragHint: l.dragHint,
  mediaSoon: l.mediaSoon,
  places: PLACES.map((p) => ({
    id: p.id,
    type: p.type,
    name: p.name,
    desc: p.desc[lang],
    credit: p.credit,
    media: mediaUrl(p.media),
    poster: p.poster ? mediaUrl(p.poster) : '',
  })),
};
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={l.description} />
    <meta name="robots" content="index, follow" />
    <meta name="theme-color" content="#141318" />
    <link rel="canonical" href={canonical} />
    {alts.map((a) => <link rel="alternate" hreflang={a.hreflang} href={a.href} />)}
    <link rel="icon" type="image/png" sizes="512x512" href="/favicon.png" />
    <link rel="icon" href="/favicon.ico" />
    <title>{l.title}</title>

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content="ISO Nord" />
    <meta property="og:title" content={l.title} />
    <meta property="og:description" content={l.ogDescription} />
    <meta property="og:url" content={canonical} />
    <meta property="og:locale" content={ogLocale} />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Archivo:wght@300;400;500&family=Archivo+Expanded:wght@200;300;500&family=Barlow+Condensed:wght@300;400&display=swap" rel="stylesheet" />
  </head>

  <body class="l360-page">

    <a href="#l360-main" class="skip-link">{c.skipLink}</a>

    <InstrumentHud lang={lang} />

    <nav class="l360-nav" aria-label={navAria}>
      <a href={pathFor('home', lang)} class="l360-nav__home">ISO Nord</a>
      <div class="l360-nav__links">
        <a href={pathFor('labs', lang)} class="l360-nav__link">{l.backToLabs}</a>
        <a href={pathFor('portfolio', lang)} class="l360-nav__link">{c.navPortfolio}</a>
        <LangSwitch lang={lang} page="labs360" class="l360-nav__link" />
      </div>
    </nav>

    <main id="l360-main" tabindex="-1">

      <header class="l360-hero">
        <div class="l360-inner">
          <p class="l360-eyebrow">{l.eyebrow}</p>
          <h1 class="l360-title">{l.titleLines.map((line, i) => (<>{i > 0 && <br />}{line}</>))}</h1>
          <p class="l360-lede">{l.lede}</p>
        </div>
      </header>

      <section class="l360-map-section">
        <div class="l360-inner">

          <!-- Sélecteur de ville -->
          <div class="l360-cities" role="group" aria-label={l.cityAria}>
            {CITIES.map((city, i) => (
              <button
                type="button"
                class="l360-city"
                data-city-btn={city}
                aria-pressed={i === 0 ? 'true' : 'false'}
              >{l.cities[city]}</button>
            ))}
          </div>

          <!-- Carte stylisée : grille + pins en % -->
          <div class="l360-map" role="region" aria-label={l.mapAria}>
            <svg class="l360-grid" aria-hidden="true" preserveAspectRatio="none" viewBox="0 0 100 100">
              {Array.from({ length: 9 }, (_, i) => (
                <line x1={(i + 1) * 10} y1="0" x2={(i + 1) * 10} y2="100" />
              ))}
              {Array.from({ length: 9 }, (_, i) => (
                <line x1="0" y1={(i + 1) * 10} x2="100" y2={(i + 1) * 10} />
              ))}
            </svg>
            <span class="l360-coord l360-coord--tl" aria-hidden="true">46°48′N</span>
            <span class="l360-coord l360-coord--br" aria-hidden="true">71°14′W</span>

            {CITIES.map((city, ci) => (
              <div class="l360-pins" data-city={city} hidden={ci !== 0}>
                {byCity(city).map((p) => (
                  <button
                    type="button"
                    class="l360-pin"
                    data-place-id={p.id}
                    style={`left:${p.x}%; top:${p.y}%;`}
                    aria-haspopup="dialog"
                  >
                    <span class="l360-pin__ring" aria-hidden="true"></span>
                    <span class="l360-pin__dot" aria-hidden="true"></span>
                    <span class="l360-pin__label">
                      {p.name}
                      <em class="l360-pin__badge">{p.type === '360' ? l.badge360 : l.badgeVideo}</em>
                    </span>
                  </button>
                ))}
              </div>
            ))}
          </div>

          <!-- Légende — mêmes lieux, tapables (mobile d'abord) -->
          {CITIES.map((city, ci) => (
            <ol class="l360-legend" data-legend-city={city} aria-label={l.legendAria} hidden={ci !== 0}>
              {byCity(city).map((p, i) => (
                <li>
                  <button type="button" class="l360-legend__item" data-place-id={p.id} aria-haspopup="dialog">
                    <span class="l360-legend__num">{pad(i + 1)}</span>
                    <span class="l360-legend__name">{p.name}</span>
                    <em class="l360-legend__badge">{p.type === '360' ? l.badge360 : l.badgeVideo}</em>
                  </button>
                </li>
              ))}
            </ol>
          ))}

        </div>
      </section>
    </main>

    <!-- Modal viewer — un seul overlay réutilisé pour 360 et vidéo -->
    <div class="l360-modal" id="l360-modal" role="dialog" aria-modal="true" aria-labelledby="l360-title" hidden>
      <div class="l360-modal__backdrop" data-modal-close></div>
      <div class="l360-modal__panel">
        <button type="button" class="l360-modal__close" data-modal-close aria-label={l.close}>×</button>
        <div class="l360-modal__media" id="l360-media"></div>
        <p class="l360-modal__hint" hidden>{l.dragHint}</p>
        <p class="l360-modal__fallback" hidden>{l.mediaSoon}</p>
        <div class="l360-modal__info">
          <h2 class="l360-modal__name" id="l360-title"></h2>
          <p class="l360-modal__desc"></p>
          <p class="l360-modal__credit"></p>
        </div>
      </div>
    </div>

    <footer class="l360-footer">
      <div class="l360-inner l360-footer__row">
        <span class="l360-footer__mark">ISO Nord</span>
        <div class="l360-footer__meta">
          <a href={pathFor('labs', lang)}>{l.backToLabs}</a>
          <a href={pathFor('home', lang)}>{l.footerBack}</a>
          <span>{c.copyright}</span>
        </div>
      </div>
    </footer>

    <div class="cursor-dot" aria-hidden="true"></div>
    <div class="cursor-ring" aria-hidden="true"></div>
    <div class="progress-bar" aria-hidden="true"></div>

    <script type="application/json" id="l360-data" set:html={JSON.stringify(runtime)} />
    <script src="../../scripts/labs360.js"></script>

    <style>
      /* Page sombre — tokens globaux du site (bg #141318, accent lime). */
      .l360-page { background: var(--bg); color: var(--text); min-height: 100%; }

      .l360-inner {
        max-width: 72rem;
        margin: 0 auto;
        padding: 0 clamp(1.25rem, 5vw, 6rem);
      }

      /* ── Nav (motif labs-nav, en sombre) ── */
      .l360-nav {
        position: absolute;
        top: 0; left: 0; right: 0;
        z-index: 3;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: clamp(1.4rem, 3vw, 2.2rem) clamp(1.25rem, 5vw, 6rem);
      }
      .l360-nav__home, .l360-nav__link {
        font-family: var(--font-label);
        font-size: 0.66rem;
        letter-spacing: 0.34em;
        text-transform: uppercase;
        color: var(--text);
        transition: color 0.3s ease;
      }
      .l360-nav__link { color: var(--muted); }
      .l360-nav__links { display: flex; align-items: center; gap: clamp(1.1rem, 3vw, 2rem); }
      .l360-nav__home:hover, .l360-nav__link:hover { color: var(--accent); }

      /* ── Hero ── */
      .l360-hero {
        padding: clamp(7rem, 18vh, 11rem) 0 clamp(2.5rem, 6vw, 4rem);
      }
      .l360-eyebrow {
        font-family: var(--font-label);
        font-size: 0.72rem;
        letter-spacing: 0.36em;
        text-transform: uppercase;
        color: var(--dim);
        margin-bottom: var(--s-lg);
      }
      .l360-title {
        font-family: var(--font-display);
        font-weight: 200;
        font-size: clamp(2.6rem, 8vw, 6.2rem);
        text-transform: uppercase;
        line-height: 0.94;
        text-wrap: balance;
      }
      .l360-lede {
        margin-top: var(--s-xl);
        max-width: 40ch;
        font-size: clamp(0.95rem, 1.4vw, 1.1rem);
        line-height: 1.7;
        color: var(--muted);
      }

      /* ── Sélecteur de ville ── */
      .l360-cities { display: flex; gap: var(--s-xl); margin-bottom: var(--s-lg); }
      .l360-city {
        appearance: none;
        background: none;
        border: 0;
        padding: 0.5rem 0;
        cursor: pointer;
        font-family: var(--font-label);
        font-size: clamp(0.95rem, 2vw, 1.15rem);
        letter-spacing: 0.28em;
        text-transform: uppercase;
        color: var(--dim);
        border-bottom: 1px solid transparent;
        transition: color 0.3s ease, border-color 0.3s ease;
      }
      .l360-city:hover { color: var(--text); }
      .l360-city[aria-pressed='true'] { color: var(--accent); border-color: var(--accent); }

      /* ── Carte ── */
      .l360-map {
        position: relative;
        aspect-ratio: 4 / 5;               /* mobile-first : carte haute */
        background:
          radial-gradient(120% 90% at 30% 20%, #1a1920 0%, var(--surface) 55%, #121117 100%);
        border: 1px solid var(--border);
        overflow: hidden;
      }
      .l360-map::after {                    /* grain du site */
        content: '';
        position: absolute; inset: 0;
        background: var(--grain);
        opacity: 0.05;
        pointer-events: none;
      }
      @media (min-width: 768px) { .l360-map { aspect-ratio: 16 / 10; } }

      .l360-grid { position: absolute; inset: 0; width: 100%; height: 100%; }
      .l360-grid line { stroke: var(--border); stroke-width: 0.15; }

      .l360-coord {
        position: absolute;
        font-family: var(--font-label);
        font-size: 0.6rem;
        letter-spacing: 0.24em;
        color: var(--dim);
      }
      .l360-coord--tl { top: 0.8rem; left: 1rem; }
      .l360-coord--br { bottom: 0.8rem; right: 1rem; }

      /* ── Pins ── */
      .l360-pins { position: absolute; inset: 0; }
      .l360-pin {
        appearance: none; background: none; border: 0; cursor: pointer;
        position: absolute;
        width: 44px; height: 44px;          /* cible tactile ≥ 44px */
        transform: translate(-50%, -50%);
        display: grid; place-items: center;
      }
      .l360-pin__dot {
        grid-area: 1 / 1;
        width: 9px; height: 9px;
        border-radius: 50%;
        background: var(--accent);
        box-shadow: 0 0 12px color-mix(in oklab, var(--accent) 60%, transparent);
      }
      .l360-pin__ring {
        grid-area: 1 / 1;
        width: 26px; height: 26px;
        border-radius: 50%;
        border: 1px solid color-mix(in oklab, var(--accent) 55%, transparent);
        animation: l360-pulse 2.6s ease-out infinite;
      }
      @keyframes l360-pulse {
        0% { transform: scale(0.6); opacity: 0.9; }
        70%, 100% { transform: scale(1.5); opacity: 0; }
      }
      @media (prefers-reduced-motion: reduce) {
        .l360-pin__ring { animation: none; opacity: 0.5; }
      }
      .l360-pin__label {
        position: absolute;
        bottom: calc(100% - 2px);
        left: 50%;
        transform: translateX(-50%);
        white-space: nowrap;
        font-family: var(--font-label);
        font-size: 0.66rem;
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: var(--text);
        background: color-mix(in oklab, var(--bg) 82%, transparent);
        border: 1px solid var(--border);
        padding: 0.3rem 0.55rem;
        opacity: 0;
        transition: opacity 0.25s ease;
        pointer-events: none;
      }
      .l360-pin:hover .l360-pin__label,
      .l360-pin:focus-visible .l360-pin__label { opacity: 1; }
      .l360-pin__badge, .l360-legend__badge {
        font-style: normal;
        color: var(--accent);
        margin-left: 0.5em;
      }

      /* ── Légende ── */
      .l360-legend { list-style: none; margin: var(--s-lg) 0 0; padding: 0; }
      .l360-legend__item {
        appearance: none; background: none; border: 0; cursor: pointer;
        display: flex; align-items: baseline; gap: var(--s-md);
        width: 100%;
        padding: 0.85rem 0;
        border-top: 1px solid var(--border);
        font-family: var(--font-label);
        letter-spacing: 0.2em;
        text-transform: uppercase;
        color: var(--muted);
        transition: color 0.3s ease;
        text-align: left;
      }
      .l360-legend li:last-child .l360-legend__item { border-bottom: 1px solid var(--border); }
      .l360-legend__item:hover { color: var(--text); }
      .l360-legend__num { font-size: 0.66rem; color: var(--dim); }
      .l360-legend__name { font-size: 0.85rem; }
      .l360-legend__badge { font-size: 0.66rem; }

      /* ── Modal ── */
      .l360-modal { position: fixed; inset: 0; z-index: 50; }
      .l360-modal__backdrop {
        position: absolute; inset: 0;
        background: color-mix(in oklab, var(--bg) 78%, black);
        opacity: 0.92;
      }
      .l360-modal__panel {
        position: absolute; inset: 0;
        margin: auto;
        width: min(100%, 64rem);
        height: 100%;
        display: flex;
        flex-direction: column;
        background: var(--surface);
        border: 1px solid var(--border);
        overflow: hidden;
      }
      @media (min-width: 768px) {
        .l360-modal__panel { height: min(100%, 44rem); width: min(92vw, 64rem); }
      }
      .l360-modal__close {
        appearance: none; background: color-mix(in oklab, var(--bg) 70%, transparent);
        border: 1px solid var(--border-control);
        color: var(--text);
        position: absolute; top: 0.9rem; right: 0.9rem; z-index: 3;
        width: 44px; height: 44px;
        font-size: 1.4rem; line-height: 1;
        cursor: pointer;
        transition: color 0.3s ease, border-color 0.3s ease;
      }
      .l360-modal__close:hover { color: var(--accent); border-color: var(--accent); }

      .l360-modal__media { position: relative; flex: 1; min-height: 0; background: #0c0b0f; }
      .l360-modal__media :global(video),
      .l360-modal__media :global(.pnlm-container) {
        position: absolute !important; inset: 0;
        width: 100% !important; height: 100% !important;
      }
      .l360-modal__media :global(video) { object-fit: cover; }

      .l360-modal__hint {
        position: absolute;
        left: 50%; transform: translateX(-50%);
        bottom: 8.5rem;
        z-index: 2;
        font-family: var(--font-label);
        font-size: 0.66rem;
        letter-spacing: 0.3em;
        text-transform: uppercase;
        color: var(--text);
        background: color-mix(in oklab, var(--bg) 72%, transparent);
        border: 1px solid var(--border);
        padding: 0.45rem 0.8rem;
        pointer-events: none;
      }
      .l360-modal__fallback {
        position: absolute; inset: 0; z-index: 1;
        display: grid; place-items: center;
        font-family: var(--font-label);
        letter-spacing: 0.24em;
        text-transform: uppercase;
        font-size: 0.75rem;
        color: var(--dim);
      }

      .l360-modal__info {
        padding: var(--s-lg) var(--s-xl) var(--s-xl);
        border-top: 1px solid var(--border);
      }
      .l360-modal__name {
        font-family: var(--font-display);
        font-weight: 200;
        font-size: clamp(1.3rem, 3vw, 2rem);
        text-transform: uppercase;
      }
      .l360-modal__desc {
        margin-top: 0.5rem;
        color: var(--muted);
        font-size: 0.92rem;
        line-height: 1.6;
        max-width: 60ch;
      }
      .l360-modal__credit {
        margin-top: 0.6rem;
        font-family: var(--font-label);
        font-size: 0.66rem;
        letter-spacing: 0.26em;
        text-transform: uppercase;
        color: var(--dim);
      }
      .l360-modal__credit:empty { display: none; }

      /* ── Footer (motif labs-footer, en sombre) ── */
      .l360-footer {
        border-top: 1px solid var(--border);
        padding: var(--s-3xl) 0 var(--s-2xl);
        margin-top: var(--s-4xl);
      }
      .l360-footer__row {
        display: flex; justify-content: space-between; align-items: flex-start;
        gap: var(--s-2xl); flex-wrap: wrap;
      }
      .l360-footer__mark {
        font-family: var(--font-label);
        font-size: 0.66rem; letter-spacing: 0.34em; text-transform: uppercase;
      }
      .l360-footer__meta { display: flex; flex-direction: column; align-items: flex-end; gap: 0.5rem; }
      .l360-footer__meta a, .l360-footer__meta span {
        font-family: var(--font-label);
        font-size: 0.6rem; letter-spacing: 0.24em; text-transform: uppercase;
        color: var(--dim);
        transition: color 0.3s ease;
      }
      .l360-footer__meta a:hover { color: var(--accent); }

      @media (max-width: 640px) {
        .l360-footer__row { flex-direction: column; }
        .l360-footer__meta { align-items: flex-start; }
      }
    </style>
  </body>
</html>
```

- [ ] **Step 3: Vérifier le build**

Run: `npm run build`
Expected: `10 page(s) built` — `/labs/360/index.html` et `/en/labs/360/index.html` listés. (Le script labs360.js n'existe pas encore → créer d'abord un stub vide `src/scripts/labs360.js` si le build casse, la Task 4 le remplit.)

- [ ] **Step 4: Commit**

```bash
git add src/pages/labs src/pages/en/labs src/components/pages/Labs360.astro src/scripts/labs360.js
git commit -m "feat(labs360): /labs/360 + /en/labs/360 routes and page component"
```

---

### Task 4: Interactions `src/scripts/labs360.js`

**Files:**
- Create (remplace le stub): `src/scripts/labs360.js`

**Interfaces:**
- Consumes: le DOM contractuel de la Task 3 + `#l360-data` ; `pannellum` (dynamic import `pannellum/build/pannellum.js` + css — attache `window.pannellum`).
- Produces: rien (script de page).

- [ ] **Step 1: Écrire le script complet**

```js
/**
 * Labs 360 — interactions de la page carte.
 *
 * - Lenis + reveals GSAP (mêmes réglages que labs.js)
 * - Sélecteur Québec/Montréal (cross-fade des pins, hash partageable)
 * - Modal viewer : Pannellum (dynamic import, seulement au premier pin 360)
 *   ou <video>, focus piégé, Échap/backdrop, focus restitué au déclencheur.
 */
import gsap from 'gsap';
import { ScrollTrigger } from 'gsap/ScrollTrigger';
import Lenis from 'lenis';

gsap.registerPlugin(ScrollTrigger);

const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
const DATA = JSON.parse(document.getElementById('l360-data').textContent);
const placeById = Object.fromEntries(DATA.places.map((p) => [p.id, p]));

/* ── Smooth scroll + curseur + reveals (repris de labs.js) ────────────────── */
let lenis = null;
if (!reducedMotion) {
  lenis = new Lenis({ lerp: 0.09, smoothWheel: true });
  lenis.on('scroll', ScrollTrigger.update);
  gsap.ticker.add((t) => lenis.raf(t * 1000));
  gsap.ticker.lagSmoothing(0);

  const progressBar = document.querySelector('.progress-bar');
  if (progressBar) {
    lenis.on('scroll', ({ progress }) => { progressBar.style.width = `${progress * 100}%`; });
  }

  const dot = document.querySelector('.cursor-dot');
  const ring = document.querySelector('.cursor-ring');
  if (dot && ring && window.matchMedia('(pointer: fine)').matches) {
    document.addEventListener('mousemove', (e) => {
      gsap.to(dot, { x: e.clientX, y: e.clientY, duration: 0.04, overwrite: true });
      gsap.to(ring, { x: e.clientX, y: e.clientY, duration: 0.34, ease: 'power2.out', overwrite: true });
    });
    document.querySelectorAll('a, button').forEach((el) => {
      el.addEventListener('mouseenter', () => {
        gsap.to(ring, { scale: 2.3, opacity: 0.4, duration: 0.3 });
        gsap.to(dot, { scale: 0, opacity: 0, duration: 0.2 });
      });
      el.addEventListener('mouseleave', () => {
        gsap.to(ring, { scale: 1, opacity: 1, duration: 0.38 });
        gsap.to(dot, { scale: 1, opacity: 1, duration: 0.28 });
      });
    });
  }

  const heroBits = gsap.utils.toArray('.l360-eyebrow, .l360-title, .l360-lede');
  gsap.set(heroBits, { opacity: 0, y: 40 });
  gsap.to(heroBits, { opacity: 1, y: 0, duration: 1.1, stagger: 0.12, ease: 'power3.out', delay: 0.15 });

  gsap.from('.l360-map', {
    opacity: 0, y: 32, duration: 1,
    scrollTrigger: { trigger: '.l360-map', start: 'top 85%' },
  });
}

/* ── Sélecteur de ville ───────────────────────────────────────────────────── */
const cityButtons = [...document.querySelectorAll('[data-city-btn]')];
const pinGroups = [...document.querySelectorAll('.l360-pins')];
const legends = [...document.querySelectorAll('[data-legend-city]')];

function showCity(city) {
  cityButtons.forEach((b) => b.setAttribute('aria-pressed', String(b.dataset.cityBtn === city)));
  legends.forEach((leg) => { leg.hidden = leg.dataset.legendCity !== city; });
  pinGroups.forEach((g) => {
    const active = g.dataset.city === city;
    if (active === !g.hidden) return;               // déjà dans le bon état
    if (reducedMotion) { g.hidden = !active; return; }
    if (active) {
      g.hidden = false;
      gsap.fromTo(g.querySelectorAll('.l360-pin'),
        { opacity: 0, scale: 0.6 },
        { opacity: 1, scale: 1, duration: 0.4, stagger: 0.06, ease: 'back.out(1.7)' });
    } else {
      gsap.to(g.querySelectorAll('.l360-pin'), {
        opacity: 0, scale: 0.6, duration: 0.2, ease: 'power2.in',
        onComplete: () => { g.hidden = true; },
      });
    }
  });
  history.replaceState(null, '', `#${city}`);       // état partageable
}
cityButtons.forEach((b) => b.addEventListener('click', () => showCity(b.dataset.cityBtn)));
if (location.hash === '#montreal') showCity('montreal');

/* ── Modal viewer ─────────────────────────────────────────────────────────── */
const modal = document.getElementById('l360-modal');
const panel = modal.querySelector('.l360-modal__panel');
const backdrop = modal.querySelector('.l360-modal__backdrop');
const mediaHost = document.getElementById('l360-media');
const hintEl = modal.querySelector('.l360-modal__hint');
const fallbackEl = modal.querySelector('.l360-modal__fallback');
const titleEl = document.getElementById('l360-title');
const descEl = modal.querySelector('.l360-modal__desc');
const creditEl = modal.querySelector('.l360-modal__credit');

let viewer = null;          // instance Pannellum courante
let lastTrigger = null;     // bouton à re-focus à la fermeture
let open = false;

function showFallback() {
  mediaHost.replaceChildren();
  fallbackEl.hidden = false;
  hintEl.hidden = true;
}

async function mountMedia(place) {
  fallbackEl.hidden = true;
  hintEl.hidden = true;
  mediaHost.replaceChildren();
  if (!place.media) return showFallback();

  if (place.type === '360') {
    try {
      // Lazy : Pannellum (UMD → window.pannellum) + son CSS, au premier besoin.
      await Promise.all([
        import('pannellum/build/pannellum.js'),
        import('pannellum/build/pannellum.css'),
      ]);
      const host = document.createElement('div');
      mediaHost.append(host);
      viewer = window.pannellum.viewer(host, {
        type: 'equirectangular',
        panorama: place.media,
        autoLoad: true,
        autoRotate: reducedMotion ? 0 : -2,
        showControls: false,
        compass: false,
        friction: 0.12,
      });
      viewer.on('error', showFallback);
      hintEl.hidden = false;
      // L'indice disparaît au premier drag.
      host.addEventListener('pointerdown', () => { hintEl.hidden = true; }, { once: true });
    } catch {
      showFallback();
    }
  } else {
    const video = document.createElement('video');
    video.controls = true;
    video.playsInline = true;
    video.preload = 'metadata';
    if (place.poster) video.poster = place.poster;
    video.src = place.media;
    video.addEventListener('error', showFallback, { once: true });
    mediaHost.append(video);
  }
}

function openModal(placeId, trigger) {
  const place = placeById[placeId];
  if (!place || open) return;
  open = true;
  lastTrigger = trigger;

  titleEl.textContent = place.name;
  descEl.textContent = place.desc;
  creditEl.textContent =
    place.credit && place.credit !== '[contributeur]'
      ? `${DATA.lang === 'fr' ? 'Capté par' : 'Captured by'} ${place.credit}`
      : '';

  modal.hidden = false;
  lenis?.stop();
  document.body.style.overflow = 'hidden';
  mountMedia(place);

  if (reducedMotion) {
    gsap.set([backdrop, panel], { opacity: 1, scale: 1, y: 0 });
  } else {
    // Le panneau émerge depuis le pin : origin = centre du déclencheur.
    const r = trigger.getBoundingClientRect();
    panel.style.transformOrigin =
      `${r.left + r.width / 2 - panel.getBoundingClientRect().left}px ` +
      `${r.top + r.height / 2 - panel.getBoundingClientRect().top}px`;
    gsap.fromTo(backdrop, { opacity: 0 }, { opacity: 0.92, duration: 0.35, ease: 'power2.out' });
    gsap.fromTo(panel,
      { opacity: 0, scale: 0.86 },
      { opacity: 1, scale: 1, duration: 0.5, ease: 'power3.out' });
  }
  modal.querySelector('.l360-modal__close').focus();
}

function closeModal() {
  if (!open) return;
  open = false;
  const done = () => {
    modal.hidden = true;
    viewer?.destroy();
    viewer = null;
    mediaHost.replaceChildren();          // stoppe la vidéo, libère Pannellum
    lenis?.start();
    document.body.style.overflow = '';
    lastTrigger?.focus();
  };
  if (reducedMotion) return done();
  gsap.to(panel, { opacity: 0, scale: 0.92, duration: 0.28, ease: 'power2.in' });
  gsap.to(backdrop, { opacity: 0, duration: 0.3, delay: 0.05, onComplete: done });
}

document.querySelectorAll('[data-place-id]').forEach((btn) => {
  btn.addEventListener('click', () => openModal(btn.dataset.placeId, btn));
});
modal.querySelectorAll('[data-modal-close]').forEach((el) => el.addEventListener('click', closeModal));

document.addEventListener('keydown', (e) => {
  if (!open) return;
  if (e.key === 'Escape') return closeModal();
  if (e.key !== 'Tab') return;
  // Focus piégé dans le modal.
  const focusables = [...modal.querySelectorAll('button, video, a[href], [tabindex]:not([tabindex="-1"])')]
    .filter((el) => !el.hidden && el.offsetParent !== null);
  if (!focusables.length) return;
  const first = focusables[0];
  const last = focusables[focusables.length - 1];
  if (e.shiftKey && document.activeElement === first) { last.focus(); e.preventDefault(); }
  else if (!e.shiftKey && document.activeElement === last) { first.focus(); e.preventDefault(); }
});
```

- [ ] **Step 2: Vérifier le build**

Run: `npm run build`
Expected: `10 page(s) built`, un chunk séparé pour pannellum dans `dist/_astro/` (lazy).

- [ ] **Step 3: Commit**

```bash
git add src/scripts/labs360.js
git commit -m "feat(labs360): map interactions, Pannellum lazy viewer, video modal"
```

---

### Task 5: Vérification navigateur + finitions

**Files:**
- Modify (si correctifs): `src/components/pages/Labs360.astro`, `src/scripts/labs360.js`

**Interfaces:**
- Consumes: tout ce qui précède.
- Produces: page validée, console propre.

- [ ] **Step 1: Lancer le dev server et vérifier `/labs/360`**

Vérifier via le Browser pane (`.claude/launch.json` existe déjà dans le repo — l'utiliser, sinon config `npm run dev`, port 4321) :
- pins Québec visibles, bascule Montréal (cross-fade, hash `#montreal`)
- pin 360 → Pannellum se charge, drag fonctionne, hint visible puis disparaît
- pin vidéo → lecteur, lecture OK, poster affiché
- fermeture : bouton ×, Échap, clic backdrop — focus restitué
- console : aucune erreur

- [ ] **Step 2: Vérifier mobile 375 px et `/en/labs/360`**

`resize_window` mobile → carte 4/5, légende tapable, modal plein écran. Page EN : textes anglais, LangSwitch FR↔EN pointe vers `/labs/360` ↔ `/en/labs/360`.

- [ ] **Step 3: Build final + commit des correctifs éventuels**

Run: `npm run build`
Expected: `10 page(s) built`.

```bash
git add -A
git commit -m "fix(labs360): browser-verification polish"
```
